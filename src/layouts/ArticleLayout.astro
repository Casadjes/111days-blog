<article class="article-content mb-20">
  <slot />
</article>

<style>
  :global(.article-content) {
    line-height: 1.4;
    text-wrap: pretty;
    color: #333;
  }

  :global(
      .article-content h1,
      .article-content h2,
      .article-content h3,
      .article-content h4,
      .article-content h5,
      .article-content h6
    ) {
    font-weight: 700;
    margin-top: 1.5rem;
    margin-bottom: 1rem;
    line-height: 1.3;
  }

  :global(.article-content h1) {
    font-size: 2.25rem;
  }

  :global(.article-content h2) {
    font-size: 1.875rem;
    color: #6366f1; /* Color para h2 */
  }

  :global(.article-content h3) {
    font-size: 1.5rem;
  }

  :global(.article-content h4) {
    font-size: 1.25rem;
  }

  :global(.article-content p) {
    margin: 1.25rem 0;
    font-size: 1.125rem;
  }

  :global(.article-content a) {
    color: #6366f1;
    text-decoration: underline;
    transition: color 0.3s ease;
  }

  :global(.article-content ul) {
    list-style: none; /* Elimina el estilo predeterminado */
    padding-left: 1.25rem; /* Espacio para los elementos de la lista */
    margin-bottom: 1.25rem;
  }

  :global(.article-content ul li) {
    position: relative;
    margin-bottom: 0.75rem;
    padding-left: 1rem; /* Espacio para el punto */
  }

  :global(.article-content ul li::before) {
    content: "•"; /* Punto personalizado */
    position: absolute;
    left: 0;
    color: #818cf8; /* Color del punto igual al de h2 */
    font-size: 1rem; /* Tamaño del punto */
  }

  :global(.article-content blockquote) {
    border-left: 4px solid #60a5fa;
    padding-left: 1rem;
    color: #6b7280;
    font-style: italic;
    margin: 1.5rem 0;
  }

  :global(.article-content pre) {
    border-radius: 8px;
    padding: 1.25rem;
    white-space: pre-wrap;
    word-break: break-word;
    overflow-x: auto;
  }

  :global(.article-content code) {
    background-color: hsla(231, 15%, 18%, 0.14);
    padding: 2px 5px;
    border-radius: 4px;
    font-family: monospace;
    font-size: 0.875rem;
  }

  :global(.article-content img) {
    max-width: 100%;
    border-radius: 8px;
    margin: 1.25rem 0;
  }

  :global(.article-content table) {
    width: 100%;
    border-collapse: collapse;
    margin: 1.25rem 0;
  }

  :global(.article-content table th, .article-content table td) {
    padding: 0.75rem;
    border: 1px solid #ddd;
  }

  :global(.article-content table th) {
    background-color: #f1f5f9;
    font-weight: 600;
    text-align: left;
  }

  :global(.article-content table td) {
    background-color: #fff;
  }

  /* Fine-tuning lists inside blockquotes */
  :global(.article-content blockquote ul, .article-content blockquote ol) {
    margin-top: 0.5rem;
  }
</style>
